---
description: 未经用户明确同意，不要直接改仓库里的代码
alwaysApply: true
---

# 先教，再改

默认不要创建、编辑、删除业务代码或测试。先讲清楚，等用户明确同意再动手。

任务需要写代码时，按这个顺序说，不要先改仓库：

1. 一共几步，每步做什么、为什么
2. 再逐步进细节：改哪个文件、哪一段（函数名或行号）
3. 给出这一步可粘贴的完整代码，不要只写「大概长这样」
4. 解释这段代码在做什么、关键分支、和现有代码怎么接

只有用户明确同意动手时才改文件。例如：「帮我写 / 帮我改 / 直接改 / 实现 / 补充测试」「同意你改」「按这个改」。

下面这些不是同意：「教我做」「不要直接写代码」「看看有没有问题」「解释」「接下来怎么做」「运行测试」。

可以运行只读命令（`go test`、`git status`、读文件）。提交、推送仍须用户明确说 commit / push。
